# 认知科学导论大作业说明文档

## 一、任务目标

本项目面向事件相关 EEG 片段二分类任务，目标是根据单个脑电样本判断其类别：

- `background`
- `target`

每个样本以 `.npy` 文件保存，形状为 `1 x 59 x 282`，可理解为 59 个 EEG 通道在 282 个时间点上的一次事件响应记录。训练集提供标签，测试集标签不公开。最终需要训练模型，并对测试集生成：

- `res/predictions.csv`

提交文件至少包含两列：

- `eeg_file`
- `prediction`

## 二、当前工程结构

```text
cognitivescience-assignment/
├── data/
│   ├── train/
│   ├── test/
│   └── train_labels.csv
├── models/
│   ├── tabular_ensemble.joblib
│   ├── best_model.pth
│   └── final_model.pth
├── res/
│   ├── predictions.csv
│   ├── prediction_probabilities.csv
│   ├── tabular_validation.csv
│   ├── threshold_scan.csv
│   └── sample_predictions.csv
├── assignment/
│   ├── body.tex
│   └── figures/
├── feature_engineering.py
├── load_data.py
├── model.py
├── train.py
├── test.py
├── utils.py
├── requirements.txt
└── 认知科学导论大作业说明文档.md
```

主要文件说明：

- `feature_engineering.py`：构建当前主模型使用的 1022 维表格特征。
- `train.py`：训练 LightGBM 多随机种子集成模型，并执行跨 session 验证和阈值扫描。
- `test.py`：加载模型并生成测试集预测。
- `model.py`：保留 EEGNet-style 神经网络结构，作为可选深度学习分支；当前最终主模型不依赖该分支。
- `res/tabular_validation.csv`：跨 session 验证结果。
- `res/threshold_scan.csv`：候选阈值扫描结果。
- `res/prediction_probabilities.csv`：测试集 target 概率、阈值、预测规则等辅助信息。
- `assignment/body.tex`：大作业报告正文。

## 三、数据说明

训练数据位于：

- `data/train/`
- `data/train_labels.csv`

测试数据位于：

- `data/test/`

`train_labels.csv` 至少包含：

- `eeg_file`
- `label`

当前标签分布为：

- `background`: 900
- `target`: 794

训练集覆盖 5 名被试，每名被试包含 `sess1` 和 `sess2`；测试集为同 5 名被试的 `sess3`。因此，本项目重点关注跨 session 泛化能力，而不是随机划分下的表面准确率。

## 四、当前主模型方案

当前最终方案不是直接使用原始 EEG 矩阵训练深度网络，而是采用：

```text
EEG 原始信号 + 文件名序列信息
        ↓
1022 维表格特征
        ↓
5 个 LightGBM 基模型
        ↓
概率平均集成
        ↓
稳定阈值预测
```

### 1. 特征工程

`feature_engineering.py` 构建两类特征：

- 元信息与序列特征：被试编号、epoch、rank、前后 gap、局部密度、周期 harmonic 特征等。
- EEG 信号统计特征：全局均值/标准差/绝对均值，多个时间窗的通道级均值与标准差。

最终每个样本得到 1022 维特征。

为了增强对 `sess3` 的泛化能力，当前实现中保留 session 内派生特征，如 rank、gap、local density，但将原始 session 编号本身置为常数，避免模型在全量训练时学习 `sess1/sess2` 的数值分裂并外推到未见过的 `sess3`。

### 2. 模型配置

当前 LightGBM 基模型主要参数：

- `n_estimators=300`
- `num_leaves=10`
- `learning_rate=0.025`
- `subsample=0.92`
- `colsample_bytree=0.82`
- `reg_lambda=2.0`
- `reg_alpha=0.15`
- `min_child_samples=10`

训练 5 个不同随机种子的 LightGBM 模型，并对 target 概率取平均。

### 3. 阈值选择

默认 0.50 阈值不是直接用于最终提交。训练脚本会扫描候选阈值，并按以下原则选择最终阈值：

1. 优先提高两个跨 session 验证方向中的较弱方向。
2. 若较弱方向相同，再比较两个方向的平均 accuracy。
3. 若仍相同，选择更接近 0.50 的阈值。

当前自动选择的最终阈值为：

- `0.445`

## 五、验证方案

测试集是 `sess3`，因此随机划分训练/验证集会高估泛化能力。当前验证方案采用跨 session 验证：

- 使用所有被试的 `sess1` 训练，在 `sess2` 验证。
- 使用所有被试的 `sess2` 训练，在 `sess1` 验证。

该方案不能完全等价于隐藏测试集，但比随机划分更接近最终提交场景。

当前集成模型验证结果：

| 验证方向 | 0.50 Accuracy | 0.445 Accuracy | ROC-AUC | 90-background 校准 Accuracy |
|---|---:|---:|---:|---:|
| Session1 -> Session2 | 0.8146 | 0.8134 | 0.8867 | 0.8028 |
| Session2 -> Session1 | 0.7815 | 0.7922 | 0.8575 | 0.7767 |
| 平均 | 0.7980 | 0.8028 | 0.8721 | 0.7898 |

选择 `0.445` 阈值的原因是它提高了较弱验证方向的准确率，更符合最终 `sess3` 分布未知时的稳健策略。

## 六、运行方式

### 1. 安装依赖

主流程最低需要：

- `numpy`
- `pandas`
- `scikit-learn`
- `lightgbm`
- `joblib`
- `matplotlib`

如使用完整环境，可执行：

```bash
pip install -r requirements.txt
```

如果只复现当前主模型，而不启用神经网络分支，则不强制依赖 `torch`。

### 2. 训练模型

```bash
python train.py
```

运行后会生成或更新：

- `models/tabular_ensemble.joblib`
- `res/tabular_validation.csv`
- `res/threshold_scan.csv`

### 3. 生成测试集预测

```bash
python test.py
```

运行后会生成或更新：

- `res/predictions.csv`
- `res/prediction_probabilities.csv`

当前测试集预测分布为：

- `background`: 485
- `target`: 357

## 七、提交内容

建议提交以下内容：

- 完整代码
- 训练好的主要模型文件：`models/tabular_ensemble.joblib`
- 测试集预测结果：`res/predictions.csv`
- 大作业报告：`assignment/body.tex` 及配套图表，或最终导出的 PDF/Word
- 讲解视频，5 分钟内

按课程要求，将所有内容以：

```text
{学生1学号}_{姓名}_{学生2学号}_{姓名}_{学生3学号}_{姓名}.zip
```

命名压缩提交。

## 八、报告内容

报告至少包含：

1. 任务理解与问题定义
2. 数据特点分析
3. 模型设计与改进思路
4. 训练方法与验证方案
5. 实验结果与误差分析
6. 小组成员分工
7. 参考文献

当前报告正文位于：

- `assignment/body.tex`

## 九、评分公式

模型效果部分参考如下准确率评分公式：

$$
\min\left(100,\max\left(60,\ 60 + 40 \times \left(\frac{acc-0.50}{topline-0.50}\right)^{\log(2.2)}\right)\right)
$$

其中：

- `topline = 0.75`
- `acc` 表示测试集准确率

含义：

- 当 `acc <= 0.50` 时，准确率部分记为 60 分。
- 当 `0.50 < acc < 0.75` 时，得分随准确率非线性增加。
- 当 `acc >= 0.75` 时，准确率部分按 100 分计。

## 十、注意事项

- 不建议使用随机划分作为主要验证依据，容易高估最终测试效果。
- 当前固定 background 数量校准在跨 session 验证中低于阈值规则，因此最终预测采用 `threshold`。
- 测试集为 `sess3`，因此应避免让模型直接依赖原始 session 编号。
- 若修改 `feature_engineering.py`，必须重新运行 `python train.py` 后再运行 `python test.py`，否则模型特征口径可能不一致。
- `res/predictions.csv` 是最终提交预测文件；`res/prediction_probabilities.csv` 仅用于分析和复核。
